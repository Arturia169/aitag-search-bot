version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: aitag-search-bot
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    environment:
      - TZ=Asia/Shanghai
